{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan migrate --force && frankenphp run --config /app/Caddyfile"
  }
}